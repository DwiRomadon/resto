<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asepfathurrahman.blacktaste">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".server.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".DetailPesananFragment" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DaftarMenu"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".DaftarPesanan" />
        <activity android:name=".InputNomerMeja" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DetailPesanan" />
        <activity android:name=".OrderFix" />
        <activity
            android:name=".tambahpesanan.DaftarMenuTambah"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>